{
  "name": "@elasticpath/textlint-rule-no-dead-relative-link",
  "version": "0.0.0-semantic-release",
  "description": "Textlint rule to validate relative links(including anchor links) in markdown documents",
  "main": "lib/no-dead-relative-link.js",
  "contributors": [
    "Parth Patel <parthopensource@gmail.com>"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/elasticpath/textlint-rule-no-dead-relative-link"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "scripts": {
    "test": "textlint-scripts test",
    "build": "textlint-scripts build",
    "prepublish": "npm run --if-present build",
    "lint": "eslint src"
  },
  "dependencies": {
    "@babel/polyfill": "^7.4.4",
    "github-slugger": "^1.2.1",
    "textlint-rule-helper": "^2.1.1"
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "textlint-scripts": "^2.1.0"
  },
  "directories": {
    "test": "test"
  },
  "files": [
    "lib/",
    "src/"
  ],
  "release": {
    "branches": [
      "main"
    ]
  },
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "textlintrule",
    "textlint",
    "validate relative link",
    "validate anchor link"
  ]
}
